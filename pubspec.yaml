name: authentika
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  qr_code_scanner: ^1.0.1
  flutter_tesseract_ocr: ^0.4.26 # Utilisation de flutter_tesseract_ocr pour l'OCR
  image_picker: ^0.8.7+4 # Version valide pour image_picker
  web3dart: ^2.3.0
  http: ^0.13.6 # Gardé seulement cette version
  firebase_core: ^2.4.1
  cloud_firestore: ^4.5.0  # Firebase Firestore pour la base de données
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
