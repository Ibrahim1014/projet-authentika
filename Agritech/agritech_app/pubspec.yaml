name: agritech_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Ajouter ici d'autres dépendances si nécessaire

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/background.jpg  # Assure-toi que ceci est la seule section "flutter"
